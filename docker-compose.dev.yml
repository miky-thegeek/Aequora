version: "3.8"

services:
  aequora:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        - PYTHON_VERSION=3.11   # build argument example (if needed)
    ports:
      - "${APP_PORT:-8443}:${APP_PORT:-8443}"
    environment:
      - APP_HOST=${APP_HOST:-0.0.0.0}
      - APP_PORT=${APP_PORT:-8443}
      - fireflyIII_id=${FIREFLYIII_ID}
      - fireflyIII_secret=${FIREFLYIII_SECRET}
      - fireflyIII_url=${FIREFLYIII_URL}
      - FLASK_DEBUG=${FLASK_DEBUG:-True}  # default to True for development
    volumes:
      - .:/app
    restart: unless-stopped
